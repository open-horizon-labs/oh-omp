Context recovery was required because normal budget assembly removed all valid conversational context. The preserved user request is incomplete: the beginning and end are exact fragments, but content between them was omitted to fit the context budget. Reground from those fragments, the repository's current state, and available recall or session artifacts, then continue. Do not treat the fragments as contiguous or guess at omitted details; recover them with tools.
